rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read files (for public portfolio images)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;  // Only authenticated users can upload
    }
  }
}
